<h1>{{user.first_name}},</h1>

<p><a href="{{url_for('user', username=follower.username, _external=True)}}">{{follower.username}}</a> ahora te sigue</p>
<table>
    <tr valign="top">
        <td><img src="{{follower.avatar(50)}}"></td>
        <td>
            <a href="{{url_for('user', username=follower.username, _external=True)}}">{{follower.username}}</a><br />
            {{follower.about_me}}
        </td>
    </tr>
</table>

<p>Saludos</p>
<p>Microblogging</p>
