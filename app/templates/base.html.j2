<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    {#
    <link rel="stylesheet" href="https://unpkg.com/wingcss"/>
    <link rel="stylesheet" href="https://cdnjs.com/libraries/skeleton-framework"/>
    <link rel="stylesheet" href="https://unpkg.com/tachyons@4.9.0/css/tachyons.min.css"/>

    <link rel="stylesheet" href="spectre.min.css">
    <link rel="stylesheet" href="spectre-exp.min.css">
    <link rel="stylesheet" href="spectre-icons.min.css">

    <link rel="stylesheet" href="https://unpkg.com/purecss@1.0.0/build/pure-min.css">
    #}

    {% if title %}
    <title>{{ title }} - Microblogging</title>
    {% else %}
    <title>Inicio - Microblogging</title>
    {% endif %}
  </head>
  <body>
    <div>Microblogging:
        <a href="{{url_for('index')}}">Inicio</a>
        {% if current_user.is_authenticated %}
        | <a href="{{url_for('search')}}">Búscar</a>
        | <a href="{{url_for('user', username=current_user.username)}}">Perfil</a>
        | <a href="{{url_for('logout')}}">Salir</a>
        {% else %}
        | <a href="{{url_for('login')}}">Iniciar sesión</a>
        | <a href="{{url_for('register')}}">Registrarse</a>
        {% endif %}
    </div>
    <hr>
    {% with messages = get_flashed_messages() %}
        {% if messages %}
        <ul>
            {% for message in messages %}
            <li>{{ message }}</li>
            {% endfor %}
        </ul>
        {% endif %}
        {% endwith %}
    {% block content %}{% endblock %}
  </body>
</html>
